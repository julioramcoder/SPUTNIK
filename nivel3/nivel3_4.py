#Contador regresivo con while.

for i in range(10,0,-1):  # bucle for con argumentos range(a,b,c), inicia en a (10), termina en b (0) y "aumentos" de c (-1)
    print(f"en... {i}")   # muestra al usuario la cuenta
print("despegue")